#include "volren/lpl.h"

void buildLatticePointLocator(const XGCMesh &m) 
{
  const int nx = 512, ny = 512;
  std::vector<int> nTrianglesPerCell(nx*ny, 0);

  for (int i=0; i<nTriangles; i++) {

  }

  int n = sqrt(m.nTriangles);
  fprintf(stderr, "n=%d\n", n);
}
